No screenshots yet.

The run that produced this repo could not build the Demo target on a Simulator: Xcode was already open on an unrelated project, and the pipeline's rule is to leave it alone rather than click through someone else's work. `swift build` / `swift test` for the library passed 18/18 on Linux. A screenshot of the Benchmark tab will be added once the app has actually been run.
